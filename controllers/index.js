const {registro, verUsuario, filtrarUsuarios,eliminarUsuario, actualizarUsuario }= require('./User.controller');

module.exports={
  registro,
  verUsuario,
  filtrarUsuarios,
  eliminarUsuario,
  actualizarUsuario 
}